主页面添加输入框，获取小说尾部路径，更改小说文件夹路径结构   完成

章节排序，完成

去后缀名 完成

将角色、剧情、杂项合并到大纲界面中 完成

修改各页面创建文件方法，改为在线修改，思路：向滚动框添加label或者linetext，绑定监听ENTER建后创建，删除创建对应按钮，无需全部重载 完成

子文件夹载入添加上级按钮，按顺序排列，删除方法中判断是否是上级按钮，是则不执行删除操作 完成

实现正文创建分卷时自动创建对应角色、剧情、杂项文件夹  完成

绑定监听快捷保存键  完成

实现大纲页面保存功能 完成

增加全局快捷键进入切换屏幕尺寸  完成

定时自动保存  完成

实现窗口跳转时和上一个窗口尺寸相同：showMinimized( )- 最小化;    完成
  showMaximized() - 最大化;
  showFullscreen() - 窗口全屏显示，不带标题栏和边框。
  showNormal() - 回到窗口的原始尺寸。
    isMinimized () - 如果窗口最小化，返回值为True;否则，为False;
  isMaximized() - 如果窗口最大化，返回值为True;否则，为False;
  isFullScreen( ) - 如果窗口全屏显示，返回值为True;否则，为False;
  isActiveWindow() - 如果是活动窗口，返回值为True;否则，为False;;
  windowstate() - 返回窗口状态的组合值.

为三个各页面添加快捷键选中打开按钮，键盘操作， 实现无鼠标操作【重写kespressevent来完成】， ——更改为为工具栏添加快捷键选中按钮 完成

排版、精简代码，删除不必要的资源， 添加注释      完成

重新为目录文件展示区排版，实现按钮无缝隙配许，可尝试用空白组件填充滚动框其他区域 -

添加新按钮——安全锁，用于打开可删除文件夹的权限